// ~/public_html/backend/controllers/adminStatsController.js
// (version robuste que je t’ai fournie plus haut)


// ~/public_html/backend/routes/adminStats.routes.js  (final)
import express from "express";
import { verifyAdminToken } from "../middlewares/verifyAdminToken.js";
import {
  getAdminStats,
  getCandidaturesParMois,
  getAdminStatsResume,
} from "../controllers/adminStatsController.js";

const router = express.Router();

router.get("/health", verifyAdminToken, (req, res) => res.json({ ok: true }));
router.get("/", verifyAdminToken, getAdminStats);
router.get("/candidatures-par-mois", verifyAdminToken, getCandidaturesParMois);
router.get("/resume", verifyAdminToken, getAdminStatsResume);

export default router;
